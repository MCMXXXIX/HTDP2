;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname Ex2) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ())))
;Exercise 2. Add the following two lines to the definitions area:

; (define prefix "hello")
; (define suffix "world") and add an "_" between them.

(define preffix "hello")
(define suffix "world")
(define underscore "_")

(define phrase (string-append preffix underscore suffix))

phrase
